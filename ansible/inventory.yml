all:
  hosts:
    ec2-server:
      ansible_host: <IP_ADDRESS>
      ansible_user: ubuntu
      ansible_ssh_private_key_file: /path/to/your/private/key.pem

  children:
    servers:
      hosts:
        ec2-server: